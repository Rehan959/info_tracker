# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Auth (JWT) - Optional since we removed auth
JWT_SECRET=change_me_in_production

# Python Scraper Service (Free alternative to paid APIs)
PYTHON_SCRAPER_URL=http://localhost:8000

# Free Alternatives (No API keys needed!)
# - Social Media: Direct scraping with fallbacks
# - AI: Hugging Face Inference API (free tier)
# - Analytics: Optional

# Optional: Analytics
NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id

# Optional: Email (for notifications)
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password

# Note: All core functionality now works without paid API keys!
# The app uses:
# - Direct web scraping for social media data
# - Hugging Face free AI models
# - Built-in fallbacks for reliability
